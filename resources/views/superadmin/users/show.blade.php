@extends('layouts.superadmin')
@section('title', 'Detail User') @section('page_title', 'Detail User') @section('page_subtitle', 'Informasi akun terdaftar.')
@section('content')<div class="max-w-3xl bg-white rounded-3xl border border-slate-100 shadow-sm p-8"><div class="flex items-center gap-5 mb-8"><img src="{{ $user->avatar ?: 'https://ui-avatars.com/api/?name=' . urlencode($user->name) }}" class="w-20 h-20 rounded-2xl"><div><h2 class="text-2xl font-black">{{ $user->name }}</h2><p class="text-slate-500">{{ $user->email }}</p></div></div><div class="grid md:grid-cols-2 gap-4"><div class="p-5 bg-slate-50 rounded-2xl"><p class="text-xs uppercase font-bold text-slate-400">Role</p><p class="font-bold mt-2">{{ $user->role === 'partner' ? 'Tenant' : str_replace('_', ' ', $user->role) }}</p></div><div class="p-5 bg-slate-50 rounded-2xl"><p class="text-xs uppercase font-bold text-slate-400">Status</p><p class="font-bold mt-2">{{ $user->status ?? 'active' }}</p></div><div class="p-5 bg-slate-50 rounded-2xl"><p class="text-xs uppercase font-bold text-slate-400">Tanggal Daftar</p><p class="font-bold mt-2">{{ $user->created_at?->format('d M Y, H:i') ?? '-' }}</p></div></div><a href="{{ route('superadmin.users.index') }}" class="inline-block mt-8 text-indigo-600 font-bold">Kembali</a></div>@endsection