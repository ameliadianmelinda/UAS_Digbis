@extends('layouts.superadmin')
@section('title', 'Detail Tenant') @section('page_title', 'Detail Tenant') @section('page_subtitle', 'Informasi dan performa tenant.')
@section('content')<div class="bg-white rounded-3xl border border-slate-100 shadow-sm p-8 mb-6"><div class="flex items-center gap-5 mb-8"><img src="{{ $tenant->logo_url ?: 'https://ui-avatars.com/api/?name=' . urlencode($tenant->name) }}" class="w-20 h-20 rounded-2xl object-cover"><div><h2 class="text-2xl font-black">{{ $tenant->name }}</h2><p class="text-slate-500">{{ $tenant->user->name ?? '-' }} · {{ $tenant->email ?? $tenant->user->email ?? '-' }}</p></div></div><div class="grid md:grid-cols-2 lg:grid-cols-4 gap-4"><div class="p-5 bg-slate-50 rounded-2xl"><p class="text-xs uppercase font-bold text-slate-400">Status</p><p class="font-bold mt-2">{{ $tenant->status ?? 'active' }}</p></div><div class="p-5 bg-slate-50 rounded-2xl"><p class="text-xs uppercase font-bold text-slate-400">Bergabung</p><p class="font-bold mt-2">{{ $tenant->created_at?->format('d M Y') ?? '-' }}</p></div><div class="p-5 bg-slate-50 rounded-2xl"><p class="text-xs uppercase font-bold text-slate-400">Jumlah Event</p><p class="font-bold mt-2">{{ $tenant->events->count() }}</p></div><div class="p-5 bg-slate-50 rounded-2xl"><p class="text-xs uppercase font-bold text-slate-400">Tiket Terjual</p><p class="font-bold mt-2">{{ number_format($totalTicketsSold, 0, ',', '.') }}</p></div></div><p class="mt-5 font-black text-indigo-600">Total Pendapatan: Rp {{ number_format($totalRevenue, 0, ',', '.') }}</p></div><div class="bg-white rounded-3xl border border-slate-100 shadow-sm overflow-hidden"><div class="p-8 border-b"><h3 class="font-black text-xl">Daftar Event Tenant</h3></div><div class="divide-y">@forelse($tenant->events as $event)<div class="px-8 py-5 flex justify-between"><span class="font-bold">{{ $event->title }}</span><span class="text-slate-500">{{ $event->category->name ?? '-' }} · {{ $event->date->format('d M Y') }}</span></div>@empty<div class="p-8 text-center text-slate-400">Belum ada event yang terhubung.</div>@endforelse</div></div>@endsection