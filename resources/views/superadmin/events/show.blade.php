@extends('layouts.superadmin')
@section('title', 'Detail Event') @section('page_title', 'Detail Event') @section('page_subtitle', 'Informasi event dan performa penjualannya.')
@section('content')<div class="bg-white rounded-3xl border border-slate-100 shadow-sm p-8"><div class="flex flex-col md:flex-row gap-8"><img src="{{ $event->poster_path ? asset('storage/' . $event->poster_path) : 'https://placehold.co/320x220' }}" class="w-full md:w-80 h-56 rounded-2xl object-cover"><div class="flex-1"><h2 class="text-3xl font-black">{{ $event->title }}</h2><p class="text-slate-500 mt-2">{{ $event->partner->name ?? 'Tenant belum ditentukan' }} · {{ $event->category->name ?? '-' }}</p><div class="grid sm:grid-cols-2 gap-4 mt-6"><div><p class="text-xs uppercase font-bold text-slate-400">Lokasi</p><p class="font-bold mt-1">{{ $event->location }}</p></div><div><p class="text-xs uppercase font-bold text-slate-400">Tanggal</p><p class="font-bold mt-1">{{ $event->date->format('d M Y, H:i') }}</p></div><div><p class="text-xs uppercase font-bold text-slate-400">Harga Tiket</p><p class="font-bold mt-1">Rp {{ number_format($event->price, 0, ',', '.') }}</p></div><div><p class="text-xs uppercase font-bold text-slate-400">Kuota Tersisa</p><p class="font-bold mt-1">{{ number_format($event->stock, 0, ',', '.') }}</p></div></div></div></div><div class="grid md:grid-cols-3 gap-4 mt-8"><div class="p-5 bg-slate-50 rounded-2xl"><p class="text-xs uppercase font-bold text-slate-400">Tiket Terjual</p><p class="text-2xl font-black mt-2">{{ number_format($ticketsSold, 0, ',', '.') }}</p></div><div class="p-5 bg-slate-50 rounded-2xl"><p class="text-xs uppercase font-bold text-slate-400">Pendapatan</p><p class="text-2xl font-black mt-2">Rp {{ number_format($revenue, 0, ',', '.') }}</p></div><div class="p-5 bg-slate-50 rounded-2xl"><p class="text-xs uppercase font-bold text-slate-400">Status</p><p class="text-2xl font-black mt-2">{{ $event->status ?? 'active' }}</p></div></div></div>@endsection