@extends('layouts.superadmin')
@section('title', 'Kelola Event') @section('page_title', 'Kelola Event') @section('page_subtitle', 'Pantau seluruh event dari semua tenant.')
@section('content')<form method="GET" class="mb-4 flex gap-2"><input name="search" value="{{ request('search') }}" placeholder="Cari nama event..." class="flex-1 px-5 py-3 bg-white border-2 border-slate-100 rounded-2xl outline-none focus:border-indigo-600"><button class="px-5 py-3 bg-indigo-600 text-white rounded-2xl font-bold">Cari</button></form><div class="bg-white rounded-3xl border border-slate-100 shadow-sm overflow-hidden"><div class="overflow-x-auto"><table class="w-full text-left"><thead class="bg-slate-50 text-slate-400 uppercase text-[10px] font-black tracking-widest"><tr><th class="px-8 py-4">Banner</th><th class="px-8 py-4">Nama Event</th><th class="px-8 py-4">Tenant</th><th class="px-8 py-4">Kategori</th><th class="px-8 py-4">Tanggal</th><th class="px-8 py-4">Status</th><th class="px-8 py-4">Aksi</th></tr></thead><tbody class="divide-y">@forelse($events as $event)<tr class="hover:bg-slate-50"><td class="px-8 py-5"><img src="{{ $event->poster_path ? asset('storage/' . $event->poster_path) : 'https://placehold.co/80x80' }}" class="w-16 h-16 rounded-xl object-cover"></td><td class="px-8 py-5 font-bold">{{ $event->title }}</td><td class="px-8 py-5">{{ $event->partner->name ?? '-' }}</td><td class="px-8 py-5">{{ $event->category->name ?? '-' }}</td><td class="px-8 py-5 text-sm text-slate-500">{{ $event->date->format('d M Y') }}</td><td class="px-8 py-5"><span class="px-3 py-1 rounded-lg text-xs font-bold uppercase {{ ($event->status ?? 'active') === 'inactive' ? 'bg-slate-100 text-slate-600' : 'bg-green-100 text-green-700' }}">{{ $event->status ?? 'active' }}</span></td><td class="px-8 py-5"><div class="flex gap-3"><a href="{{ route('superadmin.events.show', $event) }}" class="text-indigo-600 font-bold">Detail</a>@if(($event->status ?? 'active') !== 'inactive')<form method="POST" action="{{ route('superadmin.events.disable', $event) }}">@csrf<button class="text-orange-600 font-bold">Nonaktifkan</button></form>@endif<form method="POST" action="{{ route('superadmin.events.destroy', $event) }}" onsubmit="return confirm('Hapus event ini?')">@csrf @method('DELETE')<button class="text-rose-600 font-bold">Hapus</button></form></div></td></tr>@empty<tr><td colspan="7" class="p-8 text-center text-slate-400">Belum ada event.</td></tr>@endforelse</tbody></table></div><div class="p-6">{{ $events->links() }}</div></div>@endsection