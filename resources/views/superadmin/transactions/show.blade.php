@extends('layouts.superadmin')
@section('title', 'Detail Transaksi') @section('page_title', 'Detail Transaksi') @section('page_subtitle', 'Rincian invoice transaksi.')
@section('content')<div class="max-w-3xl bg-white rounded-3xl border border-slate-100 shadow-sm p-8"><h2 class="text-2xl font-black mb-6">{{ $transaction->order_id }}</h2><div class="grid md:grid-cols-2 gap-4"><div class="p-5 bg-slate-50 rounded-2xl"><p class="text-xs uppercase font-bold text-slate-400">Event</p><p class="font-bold mt-2">{{ $transaction->event->title ?? '-' }}</p></div><div class="p-5 bg-slate-50 rounded-2xl"><p class="text-xs uppercase font-bold text-slate-400">Tenant</p><p class="font-bold mt-2">{{ $transaction->event->partner->name ?? '-' }}</p></div><div class="p-5 bg-slate-50 rounded-2xl"><p class="text-xs uppercase font-bold text-slate-400">Pembeli</p><p class="font-bold mt-2">{{ $transaction->customer_name }}<br><span class="text-sm text-slate-500">{{ $transaction->customer_email }}</span></p></div><div class="p-5 bg-slate-50 rounded-2xl"><p class="text-xs uppercase font-bold text-slate-400">Total</p><p class="font-bold mt-2">Rp {{ number_format($transaction->total_price, 0, ',', '.') }}</p></div><div class="p-5 bg-slate-50 rounded-2xl"><p class="text-xs uppercase font-bold text-slate-400">Status</p><p class="font-bold mt-2">{{ $transaction->status }}</p></div><div class="p-5 bg-slate-50 rounded-2xl"><p class="text-xs uppercase font-bold text-slate-400">Tanggal</p><p class="font-bold mt-2">{{ $transaction->created_at->format('d M Y, H:i') }}</p></div></div></div>@endsection