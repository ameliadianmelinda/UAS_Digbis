@extends('layouts.superadmin')
@section('title', 'Laporan Transaksi') @section('page_title', 'Laporan Transaksi') @section('page_subtitle', 'Lihat seluruh transaksi lintas tenant.')
@section('content')<form method="GET" class="bg-white rounded-3xl border border-slate-100 p-6 mb-6 grid md:grid-cols-5 gap-3"><select name="tenant_id" class="px-4 py-3 bg-slate-50 rounded-xl"><option value="">Semua Tenant</option>@foreach($tenants as $tenant)<option value="{{ $tenant->id }}" @selected(request('tenant_id') == $tenant->id)>{{ $tenant->name }}</option>@endforeach</select><select name="status" class="px-4 py-3 bg-slate-50 rounded-xl"><option value="">Semua Status</option>@foreach(['pending','success','settlement','expire','cancel'] as $status)<option @selected(request('status') === $status)>{{ $status }}</option>@endforeach</select><input type="date" name="date_from" value="{{ request('date_from') }}" class="px-4 py-3 bg-slate-50 rounded-xl"><input type="date" name="date_to" value="{{ request('date_to') }}" class="px-4 py-3 bg-slate-50 rounded-xl"><button class="px-5 py-3 bg-indigo-600 text-white rounded-xl font-bold">Filter</button></form><div class="bg-white rounded-3xl border border-slate-100 shadow-sm overflow-hidden"><div class="overflow-x-auto"><table class="w-full text-left"><thead class="bg-slate-50 text-slate-400 uppercase text-[10px] font-black tracking-widest"><tr><th class="px-8 py-4">Invoice</th><th class="px-8 py-4">Tenant</th><th class="px-8 py-4">Event</th><th class="px-8 py-4">Pembeli</th><th class="px-8 py-4">Total</th><th class="px-8 py-4">Status</th><th class="px-8 py-4">Tanggal</th></tr></thead><tbody class="divide-y">@forelse($transactions as $transaction)<tr class="hover:bg-slate-50"><td class="px-8 py-5"><a href="{{ route('superadmin.transactions.show', $transaction) }}" class="font-mono font-bold text-indigo-600">{{ $transaction->order_id }}</a></td><td class="px-8 py-5">{{ $transaction->event->partner->name ?? '-' }}</td><td class="px-8 py-5">{{ $transaction->event->title ?? '-' }}</td><td class="px-8 py-5">{{ $transaction->customer_name }}</td><td class="px-8 py-5 font-black">Rp {{ number_format($transaction->total_price, 0, ',', '.') }}</td><td class="px-8 py-5"><span class="px-3 py-1 rounded-lg text-xs font-bold uppercase bg-slate-100 text-slate-700">{{ $transaction->status }}</span></td><td class="px-8 py-5 text-sm text-slate-500">{{ $transaction->created_at->format('d M Y, H:i') }}</td></tr>@empty<tr><td colspan="7" class="p-8 text-center text-slate-400">Belum ada transaksi.</td></tr>@endforelse</tbody></table></div><div class="p-6">{{ $transactions->links() }}</div></div>@endsection